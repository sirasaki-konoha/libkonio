# libkonio
A C library that further simplifies file input/output operations.


## Building
requires autoconf and automake and libtools
```sh
autoreconf -fi
./configure
make && sudo make install
```

## Usage
See [tests](./test) or [examples](./examples)

